<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.desarrollo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CLIENTE.registro_cuestionario_seccion4"></activity>
        <activity android:name=".CLIENTE.registro_cuestionario_seccion3" />
        <activity android:name=".CLIENTE.registro_cuestionario_seccion2" />
        <activity android:name=".CLIENTE.registro_cuestionario">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CLIENTE.registro_autoeficacia_seccion1">
        </activity>
        <activity android:name=".CLIENTE.registroHijo" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>